<%= turbo_stream.remove dom_id(@chat) if @chat %>

<%= turbo_stream.replace(
    "dialog_flash_messages",
    partial: "shared/flash_messages", 
    locals: { 
      flash: flash,
      dialog_message: true
     }) %>