// Configure your import map in config/importmap.rb.
// Read more: https://github.com/rails/importmap-rails

// Only import files here that need to be loaded globally 
// (e.g. Turbo, Stimulus controllers, Channels).

// Avoid importing utils or modules that are only used via explicit imports elsewhere.

import "@hotwired/turbo-rails"
import "controllers"
